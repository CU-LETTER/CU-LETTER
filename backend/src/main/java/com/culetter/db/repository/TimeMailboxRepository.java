package com.culetter.db.repository;

import com.culetter.db.entity.TimeMailbox;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TimeMailboxRepository extends JpaRepository<TimeMailbox, Long> {

}
